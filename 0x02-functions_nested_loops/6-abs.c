#include "main.h"
/**
* _abs - computes the absolute value
* @i: the number input
* if - statement
* Return: the absolute value
*/
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
