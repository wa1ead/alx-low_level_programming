#include "main.h"
#include <stdio.h>
/**
 * main - entry point
 * @argc: number of arguments
 * @argv: array of arguments
 * Return: always 0
 */
int main(int arg __attribute__((unsend)), char *argv[])
{
	printf("%s\n", *argv);

	return (0);
}
